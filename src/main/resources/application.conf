akka {
  loglevel = INFO
  log-dead-letters = off
  log-dead-letters-during-shutdown = on
}

mesos-dns-client {
  mesos-dns {
    host = "localhost"
    host = ${?MESOS_DNS_HOST}
    port = 8123
    port = ${?MESOS_DNS_PORT}
  }
}